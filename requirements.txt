# Core Flask dependencies
Flask==2.3.3
Werkzeug==2.3.7
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Mail==0.9.1

# Database
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL driver (optional, for production)
PyMySQL==1.1.0  # MySQL driver (optional, for production)

# Forms
WTForms==3.1.1

# Security & Auth
PyJWT==2.8.0
bcrypt==4.1.2

# File handling
Pillow==10.1.0
pypdf==4.0.1
# libmagic bindings (cross-platform): Windows ships the DLL via -bin
python-magic==0.4.27; sys_platform != 'win32'
python-magic-bin==0.4.14; sys_platform == 'win32'

# HTTP requests (for external API calls)
requests==2.31.0

# Caching / rate limiting (optional — falls back to in-memory if no REDIS_URL)
redis==5.0.1

# Environment
python-dotenv==1.0.0

# Production WSGI server (Linux/Docker)
gunicorn==21.2.0; sys_platform != 'win32'
